<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".FireBaseGoogleLogin">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/material_dynamic_neutral70">

        <Button
            android:id="@+id/Ggle_SingIn"
            android:layout_width="203dp"
            android:layout_height="55dp"
            android:layout_marginTop="104dp"
            android:backgroundTint="@color/white"
            android:text="Sign in with Google"
            android:textColor="@color/black"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/signOut"
            android:layout_width="147dp"
            android:layout_height="49dp"
            android:layout_marginTop="60dp"
            android:backgroundTint="?attr/colorTertiaryFixed"
            android:text="SIGN OUT"
            android:textColor="@color/black"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="@+id/Ggle_SingIn"
            app:layout_constraintHorizontal_bias="0.571"
            app:layout_constraintStart_toStartOf="@+id/Ggle_SingIn"
            app:layout_constraintTop_toBottomOf="@+id/Ggle_SingIn" />

        <Button
            android:id="@+id/signOut_Disconnect"
            android:layout_width="234dp"
            android:layout_height="51dp"
            android:layout_marginTop="68dp"
            android:backgroundTint="?attr/colorTertiaryFixed"
            android:text="SIGNOUT AND DISCONNECT"
            android:textColor="@color/black"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="@+id/signOut"
            app:layout_constraintHorizontal_bias="0.494"
            app:layout_constraintStart_toStartOf="@+id/signOut"
            app:layout_constraintTop_toBottomOf="@+id/signOut" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>